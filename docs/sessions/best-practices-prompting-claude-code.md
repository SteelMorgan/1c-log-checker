# Лучшие практики составления промптов для Claude Code

**На основе:** Системного промпта Claude Code
**Дата:** 2025-11-17
**Цель:** Максимальная эффективность работы с агентом

---

## Оглавление

1. [Основные принципы](#основные-принципы)
2. [Структура эффективного промпта](#структура-эффективного-промпта)
3. [Что любит Claude Code](#что-любит-claude-code)
4. [Чего избегать](#чего-избегать)
5. [Шаблоны промптов](#шаблоны-промптов)
6. [Работа с кодом](#работа-с-кодом)
7. [Git операции](#git-операции)
8. [Навигация по проекту](#навигация-по-проекту)
9. [Отладка и исправление багов](#отладка-и-исправление-багов)
10. [Продвинутые техники](#продвинутые-техники)

---

## Основные принципы

### 1. Будьте конкретны

**❌ Плохо:**
```
"Исправь это"
"Сделай лучше"
"Проверь код"
```

**✅ Хорошо:**
```
"Исправь ошибку типа в get_actual_log_timestamp.go:46,
 где используется interface{} вместо *time.Time"

"Оптимизируй функцию SaveTechLog в save_techlog.go:23
 для работы с большими файлами (>10MB)"

"Проверь код в handlers/ на соответствие Go best practices,
 особенно error handling"
```

---

### 2. Указывайте контекст

**❌ Плохо:**
```
"Где ошибка?"
```

**✅ Хорошо:**
```
"При запуске get_actual_log_timestamp возвращается ошибка:
 'converting Datetime64 to *interface {} is unsupported'.
 Найди, где это происходит (file:line) и предложи исправление"
```

---

### 3. Разбивайте сложные задачи

**❌ Плохо:**
```
"Сделай полную систему аутентификации с OAuth2, JWT,
 refresh tokens, role-based access control и audit logging"
```

**✅ Хорошо:**
```
"Давай сделаем систему аутентификации пошагово.

Сначала:
1. Создай структуру User и модель в БД
2. Реализуй базовую регистрацию и логин
3. Добавь JWT токены

Потом обсудим OAuth2 и RBAC. Начнём с пункта 1?"
```

---

## Структура эффективного промпта

### Анатомия идеального промпта

```markdown
[Контекст] + [Конкретная задача] + [Ожидаемый формат ответа] + [Ограничения]
```

#### Пример:

```
Контекст:
Я работаю над MCP сервером для 1С логов. Сейчас реализуется функционал
backup/restore конфигурации техлога.

Задача:
Реализуй функцию RestoreTechLog в internal/handlers/restore_techlog.go,
которая восстанавливает logcfg.xml из logcfg.xml.OLD

Формат ответа:
- Покажи полный код функции
- Укажи file:line где её определить
- Объясни error handling

Ограничения:
- Используй стандартную библиотеку Go (без внешних зависимостей)
- Обрабатывай случай, когда .OLD файл не существует
- Удаляй текущий .xml перед восстановлением
```

---

## Что любит Claude Code

### 1. Явные инструкции о tool usage

**✅ Хорошо:**
```
"Найди все файлы *.go в internal/handlers/ (используй Glob),
 затем в каждом найди функции с TODO комментариями (используй Grep),
 и составь список file:line для каждого TODO"
```

**Почему хорошо:**
- Указаны конкретные инструменты (Glob, Grep)
- Понятна последовательность действий
- Чёткий ожидаемый результат

---

### 2. Запросы с file:line

**✅ Хорошо:**
```
"Посмотри на server.go:64-68 и объясни, почему handler-ы
 инициализируются именно в таком порядке"
```

**Почему хорошо:**
- Точная локация (не нужно искать)
- Экономия токенов
- Агент знает, что читать

---

### 3. Структурированные ответы

**✅ Хорошо:**
```
"Проанализируй архитектуру MCP сервера и опиши в формате:

## Структура
- Список основных компонентов с file:line

## Зависимости
- Какие пакеты используются

## Паттерны
- Какие design patterns применены и где

## Потенциальные улучшения
- 2-3 конкретных предложения"
```

---

### 4. Параллельные задачи явно

**✅ Хорошо:**
```
"Выполни параллельно:
1. Собери проект (go build)
2. Запусти линтер (go vet)
3. Проверь форматирование (gofmt)

Покажи результаты всех трёх команд"
```

**Агент выполнит:** 3 Bash tool calls в одном сообщении (параллельно)

---

## Чего избегать

### 1. Неоднозначности

**❌ Избегать:**
```
"Сделай это лучше"
"Оптимизируй код"
"Исправь проблемы"
```

**Проблема:** Агент не знает, что именно улучшить

---

### 2. Множественные несвязанные задачи

**❌ Избегать:**
```
"Реализуй аутентификацию, настрой Docker, напиши тесты,
 обнови документацию и создай PR"
```

**Проблема:** Слишком много разных задач, потеря фокуса

**✅ Лучше:**
```
"Давай сначала реализуем аутентификацию, потом обсудим следующие шаги"
```

---

### 3. Запросы на "угадывание"

**❌ Избегать:**
```
"Как ты думаешь, что здесь не так?"
```

**✅ Лучше:**
```
"Проанализируй код в file.go:50-70 на наличие:
 - Memory leaks
 - Race conditions
 - Неоптимальных алгоритмов

 Укажи file:line для каждой проблемы"
```

---

### 4. Эмоциональная окраска

**❌ Избегать:**
```
"Это ужасный код, переделай его полностью!"
```

**✅ Лучше:**
```
"Код в file.go:100-150 имеет высокую cyclomatic complexity (15+).
 Предложи рефакторинг для улучшения читаемости и тестируемости"
```

**Из системного промпта:**
> "Avoid using over-the-top validation or excessive praise when responding
> to users such as 'You're absolutely right' or similar phrases"

---

## Шаблоны промптов

### Шаблон 1: Поиск и анализ

```markdown
Найди [что] в [где].
Для каждого найденного укажи:
- file:line
- [дополнительная информация]

Формат ответа: [структура]
```

#### Пример:
```
Найди все функции, которые работают с ClickHouse в internal/handlers/.
Для каждой укажи:
- file:line определения
- Какие SQL запросы выполняет
- Обрабатываются ли ошибки

Формат: таблица markdown
```

---

### Шаблон 2: Реализация функции

```markdown
Реализуй [функцию] в [файл]:[строка].

Требования:
- [список требований]

Ограничения:
- [список ограничений]

Верни:
- Полный код
- Объяснение подхода
- Unit тест (если применимо)
```

#### Пример:
```
Реализуй функцию ValidateGUID в internal/handlers/validation.go:50.

Требования:
- Проверяет формат GUID (8-4-4-4-12 символов)
- Возвращает ValidationError при ошибке
- Поддерживает как нижний, так и верхний регистр

Ограничения:
- Только стандартная библиотека
- Без регулярных выражений (производительность)

Верни:
- Код функции
- 3-4 примера валидных и невалидных GUID
- Benchmark тест
```

---

### Шаблон 3: Отладка

```markdown
В [file:line] происходит ошибка: [текст ошибки]

Контекст: [что делается]

Шаги для воспроизведения:
1. [шаг 1]
2. [шаг 2]

Найди причину и предложи исправление
```

#### Пример:
```
В get_actual_log_timestamp.go:48 происходит ошибка:
"clickhouse [ScanRow]: converting Datetime64 to *interface {} is unsupported"

Контекст: Функция пытается получить MAX(ts) из ClickHouse

Шаги для воспроизведения:
1. Запустить MCP сервер
2. Вызвать /tools/get_actual_log_timestamp
3. Ошибка возникает при Scan

Найди причину (почему interface{} не работает) и предложи правильный тип
```

---

### Шаблон 4: Рефакторинг

```markdown
Код в [file:line-range] нуждается в рефакторинге.

Проблемы:
- [проблема 1]
- [проблема 2]

Цели рефакторинга:
- [цель 1]
- [цель 2]

Покажи:
- До (текущий код)
- После (улучшенный код)
- Что изменилось и почему
```

#### Пример:
```
Код в server.go:60-85 нуждается в рефакторинге.

Проблемы:
- Много дублирования при инициализации handlers
- Сложно добавлять новые handlers
- Нет центрального места для конфигурации

Цели рефакторинга:
- Убрать дублирование
- Сделать расширяемым (легко добавить handler)
- Улучшить читаемость

Покажи:
- Текущий код (server.go:60-85)
- Предложенный рефакторинг (можно создать отдельный файл)
- Diff с объяснениями
```

---

### Шаблон 5: Git операции

```markdown
[Операция: commit/pr/branch]

Изменения:
- [файл 1]: [что изменено]
- [файл 2]: [что изменено]

Требования к сообщению:
- [стиль проекта]

[Дополнительные инструкции]
```

#### Пример (commit):
```
Создай git commit для текущих изменений.

Изменения:
- get_actual_log_timestamp.go: исправлен баг с interface{}
- tools.json: добавлены описания новых инструментов

Требования к сообщению:
- Следуй conventional commits (fix: ...)
- Упомяни issue #123, если связано

НЕ коммить:
- Файлы в docs/sessions/ (временные)
```

#### Пример (PR):
```
Создай pull request для текущей ветки.

Изменения (проанализируй git diff master...HEAD):
- Реализованы 3 новых MCP инструмента
- Исправлен баг с ClickHouse типами
- Добавлен тестовый скрипт

Заголовок PR: "feat: implement save/restore techlog and timestamp tools"

Описание должно включать:
- Summary (что сделано)
- Test plan (как тестировать)
- Ссылки на связанные issues

База: master
```

---

## Работа с кодом

### 1. Чтение кода

**✅ Эффективно:**
```
"Прочитай следующие файлы параллельно и сравни подход к error handling:
- internal/handlers/save_techlog.go
- internal/handlers/restore_techlog.go
- internal/handlers/get_actual_log_timestamp.go

Покажи общие паттерны и различия"
```

**Агент:** Сделает 3 Read calls параллельно → сравнит

---

### 2. Редактирование

**✅ Эффективно:**
```
"В get_actual_log_timestamp.go:46 замени:

Было:
var maxTimestamp interface{}

Стало:
var maxTimestamp *time.Time

Объясни, почему это исправляет баг"
```

**Важно:** Указан exact string для замены (из Read вывода с номерами строк)

---

### 3. Создание нового кода

**✅ Эффективно:**
```
"Создай новый файл internal/handlers/backup_manager.go со структурой:

type BackupManager struct {
    configDir string
    backups   []BackupInfo
}

type BackupInfo struct {
    Timestamp time.Time
    Path      string
    Size      int64
}

Реализуй методы:
- ListBackups() ([]BackupInfo, error)
- CreateBackup(name string) error
- RestoreBackup(name string) error

Используй паттерн из save_techlog.go и restore_techlog.go"
```

---

## Git операции

### Commits

**Из системного промпта:**
> "NEVER commit changes unless the user explicitly asks you to"

**✅ Правильный запрос:**
```
"Создай git commit для исправления в get_actual_log_timestamp.go.
 Сообщение в стиле conventional commits"
```

**Агент:**
1. Выполнит `git status`, `git diff`, `git log` параллельно
2. Проанализирует изменения
3. Создаст сообщение:
   ```
   fix: correct ClickHouse Datetime64 type handling in get_actual_log_timestamp

   Changed interface{} to *time.Time to properly handle nullable timestamps
   from ClickHouse MAX() aggregation.

   🤖 Generated with Claude Code
   Co-Authored-By: Claude <noreply@anthropic.com>
   ```
4. Выполнит `git add` и `git commit`
5. Проверит `git status`

---

### Pull Requests

**✅ Правильный запрос:**
```
"Создай PR для текущей ветки.
 Проанализируй все коммиты от master до HEAD.

 Заголовок должен отражать основное изменение.
 В описании:
 - Что реализовано (bullet points)
 - Как тестировать
 - Связанные issues (если есть)

 База: master"
```

**Агент:**
1. `git status`, `git diff master...HEAD`, `git log` - параллельно
2. Анализирует ВСЕ коммиты (не только последний!)
3. Создаёт PR через `gh pr create`
4. Возвращает URL PR

---

## Навигация по проекту

### 1. Исследование структуры

**✅ Эффективно (используй Task + Explore):**
```
"Исследуй структуру пакета internal/handlers/:
- Какие handler-ы есть
- Общие паттерны
- Зависимости между ними

Уровень детализации: medium"
```

**Агент:** Запустит Explore agent → быстрое исследование

---

### 2. Поиск по паттерну

**✅ Эффективно:**
```
"Найди все файлы в internal/, где:
1. Импортируется clickhouse пакет (Grep)
2. Есть структуры с полем 'ch *clickhouse.Client'

Для каждого укажи file:line и назначение структуры"
```

---

### 3. Трассировка вызовов

**✅ Эффективно:**
```
"Проследи путь HTTP запроса /tools/save_techlog:
1. Где регистрируется endpoint (file:line)
2. Какой handler вызывается (file:line)
3. Какая бизнес-логика выполняется (file:line)
4. Какие файловые операции происходят (file:line)

Нарисуй sequence diagram в mermaid"
```

---

## Отладка и исправление багов

### Шаблон отладки

```markdown
Проблема: [описание]

Симптомы:
- [что не работает]
- [ошибки, если есть]

Ожидаемое поведение:
- [что должно быть]

Контекст:
- [когда возникает]
- [конфигурация]

Шаги воспроизведения:
1. [шаг]
2. [шаг]

Найди причину и предложи исправление
```

### Пример из нашей сессии:

```
Проблема: get_actual_log_timestamp возвращает HTTP 500

Симптомы:
- При вызове /tools/get_actual_log_timestamp получаем Internal Server Error
- В логах: "converting Datetime64 to *interface {} is unsupported"

Ожидаемое поведение:
- Должен вернуться JSON с base_id, has_data, max_timestamp

Контекст:
- ClickHouse запущен и доступен
- В БД есть данные (видны через clickhouse-client)
- Другие endpoints работают

Шаги воспроизведения:
1. Запустить MCP сервер
2. curl -X POST http://localhost:8080/tools/get_actual_log_timestamp \
   -d '{"base_id":"d723aefd-7992-420d-b5f9-a273fd4146be"}'
3. Получить 500

Найди, в какой строке происходит ошибка (file:line) и предложи исправление
```

**Результат:**
- Агент нашёл: `get_actual_log_timestamp.go:46`
- Проблема: `var maxTimestamp interface{}`
- Решение: `var maxTimestamp *time.Time`
- Исправил и пересобрал

---

## Продвинутые техники

### 1. Цепочка промптов

Разбейте сложную задачу на chain of prompts:

```
Промпт 1: "Исследуй, как реализован error handling в существующих handlers.
           Выдели 3-4 основных паттерна"

[Получаем ответ]

Промпт 2: "Основываясь на паттерне #2 из предыдущего ответа,
           реализуй error handling в новой функции BackupManager"

[Получаем реализацию]

Промпт 3: "Напиши unit тесты для error cases в BackupManager,
           покрой все ветки error handling"
```

**Преимущества:**
- Каждый шаг проще
- Можете корректировать курс
- Лучший контроль качества

---

### 2. Итеративная разработка

```
Промпт 1: "Создай минимальную рабочую версию функции X (happy path only)"

[Проверяете]

Промпт 2: "Добавь error handling для случаев:
           - файл не найден
           - нет прав на чтение
           - файл пустой"

[Проверяете]

Промпт 3: "Добавь логирование и метрики производительности"

[Проверяете]

Промпт 4: "Напиши тесты, покрытие 80%+"
```

---

### 3. Использование Skills

**Когда есть специализированный skill:**

```
"Используй skill 'go' для code review функции SaveTechLog
 в save_techlog.go:23-49.

 Проверь:
 - Соответствие Go best practices
 - Clean Architecture принципам
 - Error handling patterns

 Предложи улучшения, если нужны"
```

**Агент:**
1. Загрузит skill 'go' (расширит промпт с Go expertise)
2. Применит специализированные знания
3. Даст детальный анализ

---

### 4. Контекстные follow-ups

После получения file:line, продолжайте:

```
Промпт 1: "Где определена структура Server?"
Ответ: "internal/mcp/server.go:19"

Промпт 2: "Посмотри на server.go:19-35 (определение Server)
           и server.go:38-83 (NewServer).

           Объясни, почему handlers инициализируются внутри NewServer,
           а не передаются как параметры.

           Предложи альтернативный подход с dependency injection"
```

---

### 5. Мета-промпты

Попросите агента помочь с формулировкой:

```
"Я хочу попросить тебя реализовать систему кеширования результатов
 запросов к ClickHouse. Как мне лучше сформулировать промпт,
 чтобы ты дал максимально полезный ответ?"
```

**Агент подскажет:**
- Какие детали указать
- Какой формат ответа запросить
- Какие ограничения учесть

---

## Работа с TODO и задачами

**Из системного промпта:**
> "Use these tools VERY frequently to ensure that you are tracking your tasks"

### Когда просить создать TODO list:

**✅ Хорошие случаи:**
```
"Мне нужно:
1. Реализовать 3 новых handler-а
2. Написать тесты для них
3. Обновить документацию
4. Создать PR

Создай todo list и начнём с первого пункта"
```

**Агент:**
- Создаст structured todo list
- Разобьёт на подзадачи
- Будет отмечать completed по ходу

---

### Формат задач в TODO

```json
{
  "content": "Implement save_techlog handler",  // Что сделать
  "status": "in_progress",                      // pending/in_progress/completed
  "activeForm": "Implementing save_techlog"     // Что делается сейчас
}
```

---

## Частые ошибки и как их избежать

### Ошибка 1: Слишком общие запросы

**❌ Плохо:**
```
"Улучши производительность"
```

**✅ Хорошо:**
```
"Профилируй функцию GetTechLog (tech_log.go:50-150):
 1. Добавь time.Now() замеры
 2. Найди bottleneck
 3. Предложи оптимизацию

 Целевая производительность: <100ms на запрос"
```

---

### Ошибка 2: Забыли про контекст

**❌ Плохо:**
```
"Исправь баг" (после 50 сообщений в другой теме)
```

**✅ Хорошо:**
```
"Вернёмся к багу с get_actual_log_timestamp.
 Ты исправил тип на *time.Time в строке 46.

 Теперь проверь, правильно ли обрабатывается NULL case
 в строках 63-66"
```

---

### Ошибка 3: Не указали формат ответа

**❌ Плохо:**
```
"Проанализируй код"
```

**✅ Хорошо:**
```
"Проанализируй код в handlers/ и верни результат в формате:

| Файл | Функция | Complexity | Проблемы | Предложения |
|------|---------|------------|----------|-------------|
| ... | ... | ... | ... | ... |

Complexity считай как cyclomatic complexity"
```

---

## Чеклист идеального промпта

Перед отправкой промпта, проверьте:

- [ ] Задача конкретна и однозначна
- [ ] Указан контекст (если нужен)
- [ ] Указаны file:line (если применимо)
- [ ] Ожидаемый формат ответа ясен
- [ ] Указаны ограничения (если есть)
- [ ] Понятно, какие tools использовать (если критично)
- [ ] Разумный scope (не слишком много задач)
- [ ] Нет эмоциональной окраски
- [ ] Нет неоднозначностей

---

## Финальные советы

### 1. Начинайте просто, усложняйте итеративно

```
Сначала: "Реализуй базовую версию функции X"
Потом: "Добавь error handling"
Затем: "Добавь логирование"
Наконец: "Оптимизируй производительность"
```

---

### 2. Используйте file:line везде

```
Всегда, когда говорите о коде:
"Функция X в file.go:123"
"Баг в file.go:456"
"Паттерн из file.go:789"
```

---

### 3. Просите объяснения

```
"Реализуй функцию X и ОБЪЯСНИ:
 - Почему выбран этот подход
 - Какие альтернативы были
 - Какие trade-offs"
```

**Польза:** Вы учитесь + можете оценить решение

---

### 4. Follow the system prompt

Claude Code оптимизирован для определённого стиля работы:
- Специализированные tools (Read/Write/Edit, не bash)
- Параллельные вызовы где возможно
- Code references (file:line)
- Safety-first для git
- TodoWrite для tracking

**Работайте с этим, не против**

---

### 5. Feedback loop

Если агент делает не то:

```
"В следующий раз, когда я прошу 'найти функцию',
 пожалуйста, всегда указывай file:line в ответе"
```

Агент запомнит в рамках сессии

---

## Итог

**3 золотых правила:**

1. **Конкретность** - всегда
2. **file:line** - для кода
3. **Структура** - в промптах и ответах

**Формула успеха:**
```
Хороший промпт = Контекст + Задача + Формат ответа + Ограничения
```

**Помните:**
> "The agent is as good as your prompts"

Чем точнее и структурированнее ваши промпты,
тем лучше и полезнее ответы агента.

---

**Размер:** ~800 строк
**Источник:** Системный промпт Claude Code + практический опыт сессии
**Обновлено:** 2025-11-17
