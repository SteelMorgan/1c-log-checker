# Сравнение подходов: ibcmd vs прямой парсинг

## Дата анализа
2025-11-13

## Контекст

Обнаружен официальный скрипт 1С для работы с журналом регистрации:
https://its.1c.ru/db/metod8dev#content:6019:hdoc:tld_script

Скрипт использует утилиту `ibcmd` для получения данных журнала регистрации.

## Сравнение подходов

### 1. Официальный подход (ibcmd)

**Описание:**
- Использует утилиту `ibcmd` (часть платформы 1С)
- Вызывает команду экспорта журнала регистрации
- Получает данные в структурированном формате (вероятно XML/JSON)

**Плюсы:**
✅ Официальный метод от 1С  
✅ Поддерживает все форматы и версии платформы  
✅ Не зависит от внутреннего формата файлов  
✅ Меньше риска ошибок парсинга  
✅ Автоматически обрабатывает все версии формата  

**Минусы:**
❌ Требует установку `ibcmd` (часть платформы 1С)  
❌ Зависит от внешней утилиты  
❌ Может быть медленнее (запуск процесса)  
❌ Нужно парсить вывод `ibcmd`  

**Команда ibcmd:**
```bash
ibcmd eventlog export --infobase <guid> --from <date> --to <date> --format xml
```

### 2. Прямой парсинг .lgp файлов

**Описание:**
- Чтение файлов `.lgp` напрямую
- Парсинг текстового формата 1С
- Формат: `1CV8LOG(ver 2.0)` + записи в формате `{timestamp,level,...}`

**Плюсы:**
✅ Не требует внешних утилит  
✅ Быстрее (прямое чтение файлов)  
✅ Полный контроль над процессом  
✅ Работает даже если `ibcmd` недоступен  

**Минусы:**
❌ Зависит от формата файлов (может сломаться при изменении)  
❌ Нужно поддерживать парсинг вручную  
❌ Может не поддерживать все версии формата  
❌ Нужно обрабатывать разные версии формата (ver 2.0, 3.0, etc.)  

## Рекомендация

**Гибридный подход:**

1. **Основной путь:** Использовать `ibcmd` (как в официальном скрипте)
   - Более надежно
   - Поддерживается 1С
   - Меньше проблем с форматами

2. **Резервный путь:** Прямой парсинг `.lgp` (если `ibcmd` недоступен)
   - Для случаев, когда `ibcmd` не установлен
   - Для работы с архивными файлами
   - Для отладки

3. **Переключатель в конфигурации:**
   ```yaml
   event_log:
     method: "ibcmd"  # или "direct"
     ibcmd_path: "/opt/1cv8/x86_64/8.3.27.0/ibcmd"
   ```

## Реализация

### Этап 1: Реализовать ibcmd-подход (приоритет)
- Добавить вызов `ibcmd eventlog export`
- Парсить XML/JSON вывод
- Интегрировать с существующим pipeline

### Этап 2: Сохранить прямой парсинг (резерв)
- Оставить текущий парсер `.lgp`
- Использовать как fallback
- Для отладки и тестирования

### Этап 3: Автоматическое определение метода
- Проверять наличие `ibcmd`
- Автоматически выбирать метод
- Логировать выбранный метод

## Реализация

### ✅ Этап 1: Изучен официальный скрипт 1С

**Найденные файлы:**
- `main.py` - основной скрипт
- `settings.yml` - конфигурация
- `requirements.txt` - зависимости

**Ключевые находки:**

1. **Команда ibcmd:**
   ```bash
   ibcmd eventlog export \
     --format json \
     --follow 1000 \
     --skip-root \
     [--from <checkpoint>] \
     <event_log_dir>
   ```

2. **Формат вывода:** JSON, каждая строка - один объект события

3. **Структура события:**
   ```json
   {
     "Date": "2025-11-13T14:42:28",
     "Event": "_$InfoBase$_.Update",
     "EventPresentation": "Данные. Изменение",
     "Session": 26,
     "User": "OrpovaE",
     "UserName": "Орлова Елена",
     "Computer": "DC-RDS-DEV1C",
     "Level": "Information",
     ...
   }
   ```

4. **Чекпоинты:** YAML файл `<infobase_guid>.dat` с полем `checkpoint`

5. **Режим follow:** Реальное время с интервалом `follow-time-msec`

### ✅ Этап 2: Реализован ibcmd-читатель

**Файл:** `internal/logreader/eventlog/ibcmd_reader.go`

**Функциональность:**
- ✅ Запуск `ibcmd eventlog export` с правильными параметрами
- ✅ Парсинг JSON вывода построчно
- ✅ Управление чекпоинтами (чтение/запись YAML)
- ✅ Режим follow (real-time)
- ✅ Маппинг всех полей в `domain.EventLogRecord`
- ✅ Обработка ошибок и stderr

**Интерфейс:**
```go
reader := NewIbcmdReader(ibcmdPath, eventLogDir, clusterGUID, infobaseGUID)
reader.Open(ctx)
record, err := reader.Read(ctx)
reader.Close()
```

### 🔄 Этап 3: Добавить конфигурацию выбора метода

**TODO:**
- Добавить в `config.go`:
  - `EVENT_LOG_METHOD` (ibcmd/direct)
  - `IBCMD_PATH` (путь к ibcmd)
- Обновить `parser_service.go` для выбора метода
- Добавить fallback на прямой парсинг если ibcmd недоступен

### 🔄 Этап 4: Тестирование

**TODO:**
- Протестировать ibcmd-читатель на реальных данных
- Сравнить результаты с прямым парсингом
- Проверить работу чекпоинтов
- Проверить обработку ошибок

### ✅ Этап 5: Документация обновлена

**Созданные документы:**
- `docs/analysis/ibcmd-vs-direct-parsing.md` - сравнение подходов
- `internal/logreader/eventlog/ibcmd_reader.go` - реализация

## Выводы

**Официальный подход (ibcmd) - ПРЕДПОЧТИТЕЛЕН:**
- ✅ Использует официальную утилиту 1С
- ✅ Поддерживает все форматы и версии
- ✅ JSON формат проще парсить
- ✅ Режим follow для real-time
- ✅ Автоматические чекпоинты

**Прямой парсинг - РЕЗЕРВНЫЙ:**
- Использовать если `ibcmd` недоступен
- Для работы с архивными файлами
- Для отладки и тестирования

