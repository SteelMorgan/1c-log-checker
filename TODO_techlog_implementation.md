# План реализации сбора данных технологического журнала

**Дата создания:** 2025-11-15
**Статус:** В работе (Phase 1)

---

## 📊 РЕЗУЛЬТАТЫ ИССЛЕДОВАНИЯ ДОКУМЕНТАЦИИ

### 1. Структура каталогов технологического журнала

**Источник:** `D:\My Projects\FrameWork 1C\scraping_its\out\v8327doc\markdown\0073_Приложение_3._Описание_и_расположение_служебных_файлов.md` (раздел 3.24)

**Два варианта размещения** (атрибут `placement` в logcfg.xml):

#### A) `placement="folders"` (иерархическая, по умолчанию)
```
D:\TechLogs\
├── rphost_1234\        # appName_pid (процесс+PID)
│   ├── 25011408.log    # yymmddhh.log (timestamp)
│   ├── 25011409.log
│   └── 25011410.log.zip
├── 1cv8c_5678\
│   └── 25011408.log
```

**Структура имени каталога:** `<appName>_<pid>`
- `appName`: rphost, 1cv8, 1cv8c, ragent и т.д.
- `pid`: идентификатор процесса ОС

**Структура имени файла:** `<yymmddhh>.log[.counter][.zip]`
- `yy`: последние 2 цифры года
- `mm`: месяц (01-12)
- `dd`: день (01-31)
- `hh`: час (00-23), кратный периоду ротации
- `counter`: монотонно возрастающий счетчик (для rotation="size")
- `zip`: если включено сжатие

**ВАЖНО:** Каталоги именуются по процессу + PID, **НЕ** по кластеру/базе!

#### B) `placement="plain"` (плоская структура)
```
D:\TechLogs\
├── rphost_1234_25011408.log
├── rphost_1234_25011409.log
├── 1cv8c_5678_25011408.log
```

**Структура имени файла:** `<appName>_<pid>_<yymmddhh>.log[.counter][.zip]`

---

### 2. Форматы файлов

**Атрибут `format` в logcfg.xml** (по умолчанию: `text`)

#### A) `format="text"` (текстовый формат)

**Hierarchical** (для `placement="folders"`):
```
45:31.831006-1,SCALL,2,level=INFO,process=1cv8,OSThread=13476,ClientID=1,...
```

**Формат строки:** `mm:ss.tttttt-d,EVENT,depth,properties...`

Где:
- `mm`: минуты в текущем часе (00-59)
- `ss`: секунды в текущей минуте (00-59)
- `tttttt`: микросекунды (000000-999999)
- `d`: длительность события в микросекундах
- `EVENT`: тип события (SCALL, EXCP, DBMSSQL и т.д.)
- `depth`: уровень вложенности (0-255)
- `properties`: пары key=value, разделенные запятыми

**Проблема:** Дата не содержится в строке! Извлекается из имени файла `yymmddhh.log`

**Plain** (для `placement="plain"`):
```
2023-08-01T15:01:45.259000-14998,SCALL,0,level=INFO,process=1cv8c,...
```

**Формат строки:** `YYYY-MM-DDTHH:MM:SS.mmmmmm-duration,EVENT,depth,properties...`

**Преимущество:** Полная дата в каждой строке (формат ISO 8601)

#### B) `format="json"` (JSON формат)
```json
{"ts":"2023-08-02T08:48:05.982000","duration":"46998","name":"SCALL","level":"INFO",...}
```

**Преимущества:**
- ✅ Проще парсить (стандартный JSON)
- ✅ Меньше токенов для AI агента
- ✅ Все поля именованы явно
- ✅ Полная дата в каждой записи

**РЕКОМЕНДАЦИЯ:** Указать агенту `format="json"` как предпочтительный формат

---

### 3. КРИТИЧЕСКАЯ ПРОБЛЕМА: ClusterGUID/InfobaseGUID

**Вывод исследования:**
Стандартная структура каталогов технологического журнала **НЕ СОДЕРЖИТ** информации о кластере/информационной базе!

**Возможные решения:**

#### Вариант A: Через параметр location в logcfg.xml
Агент создает отдельные каталоги для каждой базы:
```xml
<log location="D:\TechLogs\cluster1\base1" format="json">
    <event><eq property="name" value="EXCP"/></event>
</log>
<log location="D:\TechLogs\cluster1\base2" format="json">
    <event><eq property="name" value="EXCP"/></event>
</log>
```

Парсер определяет GUID через mapping: `path → {cluster_guid, infobase_guid}`

**Проблемы:**
- Нужен механизм настройки mapping
- Не работает если база перемещена

#### Вариант B: Через MCP параметры (ВЫБРАН)
Агент передает GUID'ы явно при настройке через `configure_techlog` MCP tool:
```json
{
  "cluster_guid": "b0881663-f2a7-4195-b7a2-f7f8e6c3a8f3",
  "infobase_guid": "d723aefd-7992-420d-b5f9-a273fd4146be",
  "location": "D:\\TechLogs",
  "format": "json",
  "events": ["EXCP", "TLOCK"]
}
```

Парсер хранит mapping: `{location → {cluster_guid, infobase_guid}}`

**Преимущества:**
- ✅ Явно и надежно
- ✅ Агент контролирует процесс
- ✅ Не зависит от структуры каталогов
- ✅ Просто реализовать

#### Вариант C: Из свойств событий
Некоторые события содержат свойства `Infobase`, `IB`, но:
- ❌ Не гарантировано для всех типов событий
- ❌ Нет cluster_guid
- ❌ Ненадежно

**ВЫБРАНО РЕШЕНИЕ: Вариант B** - через MCP параметры

---

### 4. Ротация и архивирование

#### Ротация (атрибут `rotation`)

**A) `rotation="period"` (по времени, по умолчанию)**
- Период в часах (атрибут `rotationperiod`, мин. 1 час)
- Новый файл создается каждые N часов
- Имя файла: `yymmddhh.log` (час кратен периоду)
- Пример: `25011408.log`, `25011409.log`, `25011410.log`

**B) `rotation="size"` (по размеру)**
- Размер в MB (атрибут `rotationsize`, мин. 1 MB)
- Новый файл когда текущий превысит размер
- К старому файлу добавляется `.counter`
- Пример: `rphost_1234.log`, `rphost_1234.log.1`, `rphost_1234.log.2`

#### Сжатие (атрибут `compress`)

**`compress="zip"`** - архивирует старые файлы
- Расширение: `.zip`
- Пример: `25011408.log.zip`

**РЕШЕНИЕ ДЛЯ MVP:** Пока **НЕ** обрабатывать сжатые файлы (для простоты)

#### Очистка (атрибут `history`)

Автоматически удаляет файлы старше N часов (по умолчанию 24 часа)

---

### 5. Определение формата

**Из документации:**
- Формат указывается в `logcfg.xml` атрибутом `format="text|json"`
- По умолчанию: `text`

**Решение:**
1. Читать `logcfg.xml` и извлекать атрибут `format`
2. Если файл не найден или атрибут отсутствует → попробовать определить по первой строке:
   - Если начинается с `{` → JSON
   - Иначе → TEXT
3. Поддерживать оба формата в парсере

**Рекомендация агенту:**
- Использовать `format="json"` (экономия токенов, проще парсинг)

---

## 🎯 АРХИТЕКТУРНЫЕ РЕШЕНИЯ

### 1. ClusterGUID/InfobaseGUID: Вариант B
- MCP tool `configure_techlog` принимает параметры: `{cluster_guid, infobase_guid, location, ...}`
- Парсер хранит mapping в конфиге или в памяти
- При обработке файлов из `location` → подставляет соответствующие GUID'ы

### 2. Формат данных
- Читать из `logcfg.xml` (приоритет)
- Fallback: определение по первой строке
- Поддержка обоих форматов (text и json)
- Рекомендация агенту: JSON

### 3. Offset tracking (гибридная схема)
- BoltDB: `techlog_offsets` bucket
- Ключ: `{directory}#{filename}`
- Значение: `{offset_bytes, last_timestamp, last_line_number}`
- При restart → продолжить с сохраненного offset

### 4. Обработка истории
- При первом запуске (offset not found) → читать все файлы в хронологическом порядке
- При последующих запусках → читать с сохраненного offset + новые файлы

### 5. Performance metrics
Создать таблицу `parser_metrics` в ClickHouse:
```sql
CREATE TABLE logs.parser_metrics (
    timestamp DateTime,
    parser_type LowCardinality(String),  -- 'event_log' или 'tech_log'
    cluster_guid String,
    infobase_guid String,
    files_processed UInt32,
    records_parsed UInt64,
    parsing_time_ms UInt64,
    records_per_second Float64,
    start_time DateTime,
    end_time DateTime
) ENGINE = MergeTree()
PARTITION BY toYYYYMM(timestamp)
ORDER BY (parser_type, timestamp)
TTL timestamp + INTERVAL 90 DAY;
```

Хранить последние 100 запусков для анализа производительности.

---

## 📋 ДЕТАЛЬНЫЙ ПОШАГОВЫЙ ПЛАН РАБОТ

### **PHASE 1: Подготовка и исследование** ✅ ТЕКУЩИЙ ЭТАП

- [x] Изучить документацию по технологическому журналу
- [x] Определить структуру каталогов и форматы файлов
- [x] Найти решение для ClusterGUID/InfobaseGUID
- [x] Создать детальный план работ
- [ ] Зафиксировать план в файле TODO

---

### **PHASE 2: Core Implementation (Базовая реализация)**

#### 2.1. ClusterGUID/InfobaseGUID через MCP параметры

**Файлы:**
- `internal/config/config.go` - добавить TechLogMapping
- `cmd/mcp/handlers.go` - обновить configure_techlog handler

**Задачи:**
1. Добавить структуру для mapping:
   ```go
   type TechLogMapping struct {
       Location      string
       ClusterGUID   string
       InfobaseGUID  string
       Format        string  // "text" или "json"
   }
   ```

2. MCP tool `configure_techlog` сохраняет mapping:
   - В файл `configs/techlog_mapping.json`
   - Или в ClickHouse таблицу `techlog_config`

3. Парсер читает mapping при старте

**Критерий готовности:** Парсер может определить cluster_guid/infobase_guid для файлов из заданного location

#### 2.2. Определение формата (logcfg.xml + fallback)

**Файлы:**
- `internal/techlog/config_reader.go` (новый)
- `internal/techlog/tailer.go` (обновить)

**Задачи:**
1. Создать функцию `ReadLogCfgXML(path string) (*LogCfgConfig, error)`
2. Парсить атрибут `format` из `<log>` элемента
3. Fallback: определение по первой строке файла
4. Обновить Tailer для использования определенного формата

**Критерий готовности:** Парсер корректно определяет формат (text/json) из logcfg.xml

#### 2.3. Offset tracking в BoltDB

**Файлы:**
- `internal/offset/offset.go` (обновить интерфейс)
- `internal/offset/boltdb.go` (добавить techlog методы)
- `internal/techlog/tailer.go` (интеграция)

**Задачи:**
1. Добавить bucket `techlog_offsets` в BoltDB
2. Структура offset:
   ```go
   type TechLogOffset struct {
       FilePath      string
       OffsetBytes   int64
       LastTimestamp time.Time
       LastLine      int64
   }
   ```

3. Методы:
   - `GetTechLogOffset(filePath string) (*TechLogOffset, error)`
   - `SaveTechLogOffset(offset *TechLogOffset) error`

4. Tailer сохраняет offset после каждого батча (каждые 500 записей)

**Критерий готовности:** При restart парсер продолжает с сохраненного offset

#### 2.4. Извлечение timestamp из имени файла

**Файлы:**
- `internal/techlog/text_parser.go` (обновить parseHierarchicalTimestamp)
- `internal/techlog/filename_parser.go` (новый)

**Задачи:**
1. Создать функцию `ExtractTimestampFromFilename(filename string) (time.Time, error)`
2. Парсинг форматов:
   - `yymmddhh.log` → `20YY-MM-DD HH:00:00`
   - `rphost_pid_yymmddhh.log` → аналогично

3. Обновить `parseHierarchicalTimestamp()`:
   ```go
   func parseHierarchicalTimestamp(line string, record *domain.TechLogRecord, fileTimestamp time.Time) (string, error) {
       // Использовать fileTimestamp вместо time.Now()
       record.Timestamp = time.Date(
           fileTimestamp.Year(), fileTimestamp.Month(), fileTimestamp.Day(),
           fileTimestamp.Hour(), minutes, seconds, microsec*1000,
           time.Local,
       )
   }
   ```

**Критерий готовности:** Hierarchical format имеет корректную дату из имени файла

#### 2.5. Обработка исторических файлов

**Файлы:**
- `internal/techlog/tailer.go` (переписать логику)
- `internal/techlog/file_scanner.go` (новый)

**Задачи:**
1. Создать `FileScanner` для поиска всех файлов в директории:
   ```go
   func ScanDirectory(dir string) ([]*FileInfo, error)
   // Возвращает список файлов отсортированных по времени
   ```

2. Обновить Tailer:
   - При старте: проверить offset для каждого файла
   - Если offset нет → обработать файл с начала
   - Если offset есть → продолжить с offset
   - После обработки всех файлов → переключиться в live tailing mode

3. Обработка в хронологическом порядке (по timestamp в имени файла)

**Критерий готовности:** При первом запуске парсер обрабатывает все исторические файлы

#### 2.6. Performance metrics

**Файлы:**
- `deploy/clickhouse/init/03_create_parser_metrics.sql` (новый)
- `internal/service/parser_service.go` (обновить runTechLogTailer)

**Задачи:**
1. Создать таблицу `parser_metrics` (SQL выше)

2. В `runTechLogTailer()` добавить:
   ```go
   startTime := time.Now()
   filesProcessed := 0
   recordsParsed := 0

   // ... обработка ...

   totalTime := time.Since(startTime)
   recordsPerSec := float64(recordsParsed) / totalTime.Seconds()

   // Записать метрики в ClickHouse
   writeMetrics(ctx, &ParserMetrics{
       Timestamp:        time.Now(),
       ParserType:       "tech_log",
       ClusterGUID:      mapping.ClusterGUID,
       InfobaseGUID:     mapping.InfobaseGUID,
       FilesProcessed:   filesProcessed,
       RecordsParsed:    recordsParsed,
       ParsingTimeMs:    totalTime.Milliseconds(),
       RecordsPerSecond: recordsPerSec,
       StartTime:        startTime,
       EndTime:          time.Now(),
   })

   log.Info().
       Int("files", filesProcessed).
       Int64("records", recordsParsed).
       Dur("time", totalTime).
       Float64("records_per_sec", recordsPerSec).
       Msg("Tech log parsing completed")
   ```

**Критерий готовности:** Метрики парсинга сохраняются в ClickHouse

---

### **PHASE 3: Testing & Validation (Тестирование по шагам)**

**ВАЖНО:** Пользователь проверяет каждый этап "глазами" перед переходом к следующему!

#### 3.1. Создание config файла

**Действия:**
1. Запустить MCP tool `configure_techlog` с параметрами:
   ```json
   {
     "cluster_guid": "test-cluster-guid",
     "infobase_guid": "test-infobase-guid",
     "location": "D:\\TechLogs\\test",
     "format": "json",
     "events": ["EXCP", "QERR"],
     "history": 1,
     "compress": false
   }
   ```

2. Tool создает:
   - `logcfg.xml` с настройками
   - Запись в `techlog_mapping.json`

**Проверка пользователем:**
- [ ] `logcfg.xml` создан с правильными параметрами
- [ ] `format="json"` установлен
- [ ] `location` указан правильно
- [ ] Mapping сохранен

#### 3.2. Генерация логов через unit-тесты

**Действия:**
1. Реализовать unit-тест, вызывающий целевое событие (например, деление на ноль для EXCP)
2. Запустить unit-тест в 1C
3. Подождать формирования логов

**Проверка пользователем:**
- [ ] В `D:\TechLogs\test\rphost_XXXX\` созданы .log файлы
- [ ] Файлы в JSON формате
- [ ] События EXCP присутствуют в логах
- [ ] Timestamp корректные

#### 3.3. Проверка пользователем сгенерированных логов

**Действия:**
Пользователь открывает файл лога и проверяет:
- [ ] Формат JSON корректный
- [ ] События соответствуют выполненному unit-тесту
- [ ] Все обязательные поля присутствуют
- [ ] Timestamp соответствует времени выполнения теста

#### 3.4. Отладка парсинга

**Действия:**
1. Запустить парсер с `DEBUG_SAVE_ALL_RECORDS=true`
2. Проверить parsing в логах парсера
3. Проверить записи в ClickHouse

**Проверка:**
- [ ] Парсер находит файлы
- [ ] Определяет формат как JSON
- [ ] Парсит записи без ошибок
- [ ] cluster_guid/infobase_guid подставлены корректно
- [ ] Записи попали в таблицу `tech_log`
- [ ] Метрики записались в `parser_metrics`

**Сравнение:**
- [ ] Количество записей в файле = количеству в ClickHouse
- [ ] Timestamp совпадают
- [ ] Все свойства сохранены

#### 3.5. Проверка disable_techlog tool

**Действия:**
1. Вызвать MCP tool `disable_techlog`
2. Проверить что:
   - [ ] `logcfg.xml` удален ИЛИ
   - [ ] `logcfg.xml` заменен на disabled config:
     ```xml
     <config xmlns="http://v8.1c.ru/v8/tech-log">
         <dump create="false"/>
     </config>
     ```

#### 3.6. Проверка get_tech_log tool

**Действия:**
1. Вызвать MCP tool `get_tech_log` с параметрами:
   ```json
   {
     "cluster_guid": "test-cluster-guid",
     "infobase_guid": "test-infobase-guid",
     "from": "2025-11-15T12:00:00Z",
     "to": "2025-11-15T13:00:00Z",
     "event_types": ["EXCP"],
     "limit": 100
   }
   ```

2. Проверить ответ:
   - [ ] События соответствуют фильтрам
   - [ ] Timestamp в заданном диапазоне
   - [ ] Данные полные и корректные

#### 3.7. Финальная проверка workflow

**Полный цикл:**
1. [ ] Создать config через `configure_techlog`
2. [ ] Сгенерировать события через unit-тест
3. [ ] Парсер обработал логи автоматически
4. [ ] Получить данные через `get_tech_log`
5. [ ] Проанализировать данные
6. [ ] Отключить ТЖ через `disable_techlog`
7. [ ] Проверить что ТЖ отключен

**Проверка производительности:**
- [ ] Замерить время парсинга N файлов/записей
- [ ] Проверить метрики в `parser_metrics`
- [ ] Records per second >= XXX (определить baseline)

---

### **PHASE 4: Advanced Features (Опциональные улучшения)**

#### 4.1. Параллельная обработка файлов

**Цель:** Обрабатывать несколько файлов одновременно

**Реализация:**
```go
func (t *Tailer) processFilesParallel(ctx context.Context, files []*FileInfo, handler func(*domain.TechLogRecord) error) error {
    workers := runtime.NumCPU()
    fileChan := make(chan *FileInfo, len(files))

    // Worker pool
    var wg sync.WaitGroup
    for i := 0; i < workers; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            for file := range fileChan {
                t.processFile(ctx, file, handler)
            }
        }()
    }

    // Отправить файлы в канал
    for _, file := range files {
        fileChan <- file
    }
    close(fileChan)

    wg.Wait()
    return nil
}
```

**Критерий готовности:** Обработка N файлов в X раз быстрее

#### 4.2. Обработка сжатых файлов (.zip)

**Реализация:**
- Поддержка чтения .zip файлов в `tailer.openFile()`
- Уже есть поддержка .gz в текущем коде
- Добавить .zip через `archive/zip`

**Критерий готовности:** Парсер читает архивированные файлы

---

## 🔍 ТЕКУЩИЙ СТАТУС РЕАЛИЗАЦИИ

### ✅ ЧТО УЖЕ ЕСТЬ:

1. **Парсеры:**
   - [x] `text_parser.go` - парсинг текстового формата (plain + hierarchical)
   - [x] `json_parser.go` - парсинг JSON формата
   - [x] Поддержка динамических свойств

2. **Файловая система:**
   - [x] `tailer.go` - мониторинг директории
   - [x] Обработка ротации файлов
   - [x] Поддержка .gz сжатия
   - [x] Polling каждые 500ms

3. **ClickHouse:**
   - [x] Таблица `tech_log` с индексами
   - [x] `flushTechLog()` с дедупликацией
   - [x] Поддержка динамических свойств (Arrays)

4. **Интеграция:**
   - [x] `runTechLogTailer()` в parser_service.go

### ⚠️ ЧТО НУЖНО ДОРАБОТАТЬ:

1. **ClusterGUID/InfobaseGUID:**
   - [ ] Пустые поля (parser_service.go:354-355)
   - [ ] Нужен механизм mapping через MCP параметры

2. **Формат:**
   - [ ] Hardcoded `isJSON = false` (parser_service.go:349)
   - [ ] Нужно читать из logcfg.xml

3. **Offset tracking:**
   - [ ] НЕТ offset storage для techlog
   - [ ] При restart читает с начала файла

4. **Исторические файлы:**
   - [ ] Обрабатывает только последний файл (tailer.go:156)
   - [ ] Нужна обработка всех файлов при первом запуске

5. **Timestamp:**
   - [ ] TODO в text_parser.go:149
   - [ ] Использует `time.Now()` вместо даты из имени файла

6. **Метрики:**
   - [ ] НЕТ замеров производительности
   - [ ] Нужна таблица `parser_metrics`

---

## 📅 ОЦЕНКА СРОКОВ

| Фаза | Задачи | Оценка времени |
|------|--------|----------------|
| Phase 1 | Исследование и планирование | ✅ Завершено |
| Phase 2.1 | ClusterGUID/InfobaseGUID mapping | 2-3 часа |
| Phase 2.2 | Определение формата | 1-2 часа |
| Phase 2.3 | Offset tracking | 2-3 часа |
| Phase 2.4 | Timestamp из имени файла | 1 час |
| Phase 2.5 | Обработка истории | 3-4 часа |
| Phase 2.6 | Performance metrics | 2 часа |
| Phase 3 | Тестирование (пошагово) | 4-6 часов |
| Phase 4 | Опциональные улучшения | 4-6 часов |
| **ИТОГО (MVP)** | Phase 1-3 | **15-21 час** |
| **ИТОГО (Full)** | Phase 1-4 | **19-27 часов** |

---

## 🎯 КРИТЕРИИ ПРИЕМКИ (Definition of Done)

### Минимальный набор (MVP):
- [ ] Парсер определяет cluster_guid/infobase_guid из MCP параметров
- [ ] Парсер автоматически определяет формат (text/json)
- [ ] Offset tracking работает (restart продолжает с сохраненной позиции)
- [ ] Обрабатываются исторические файлы при первом запуске
- [ ] Timestamp извлекается из имени файла для hierarchical формата
- [ ] Метрики производительности сохраняются в ClickHouse
- [ ] Все 7 этапов тестирования пройдены успешно
- [ ] Пользователь подтвердил корректность работы

### Полный набор (Full):
- [ ] Все критерии MVP выполнены
- [ ] Параллельная обработка файлов реализована
- [ ] Поддержка .zip архивов реализована
- [ ] Unit-тесты для всех компонентов
- [ ] Документация обновлена

---

## 📝 ЗАМЕТКИ И ВОПРОСЫ

### Решенные вопросы:
1. ✅ **ClusterGUID/InfobaseGUID:** Через MCP параметры (Вариант B)
2. ✅ **Формат:** Читать из logcfg.xml + fallback по первой строке
3. ✅ **Рекомендация агенту:** JSON формат (экономия токенов)
4. ✅ **Архивы:** Пока не обрабатывать (MVP без .zip)

### Открытые вопросы:
1. ❓ Хранить mapping в файле или в ClickHouse?
   - **Предложение:** Файл `configs/techlog_mapping.json` (проще для MVP)

2. ❓ Как часто сохранять offset?
   - **Предложение:** После каждого батча (500 записей) или каждые 5 секунд

3. ❓ Baseline для производительности (records per second)?
   - **Предложение:** Измерить на первом прогоне, установить как baseline

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- Skill: `.claude/skills/techlog.md` - руководство для AI агента
- Docs: `docs/techlog/logcfg.md` - описание logcfg.xml
- Platform: `D:\My Projects\FrameWork 1C\scraping_its\out\v8327doc\markdown\0073_*.md` - официальная документация
- Code: `internal/techlog/` - существующая реализация парсеров

---

## 📊 PROGRESS TRACKER

```
Phase 1: Исследование     [████████████████████] 100% ✅
Phase 2: Реализация       [░░░░░░░░░░░░░░░░░░░░]   0%
Phase 3: Тестирование     [░░░░░░░░░░░░░░░░░░░░]   0%
Phase 4: Улучшения        [░░░░░░░░░░░░░░░░░░░░]   0%
```

**Последнее обновление:** 2025-11-15
**Следующий шаг:** Phase 2.1 - ClusterGUID/InfobaseGUID mapping
